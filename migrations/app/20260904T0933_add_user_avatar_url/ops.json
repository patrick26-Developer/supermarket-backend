[
  {
    "id": "column.public.users.avatarUrl",
    "label": "Add column \"avatarUrl\" to \"users\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "avatarUrl",
        "table": "users"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"avatarUrl\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "users",
          "avatarUrl"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"avatarUrl\"",
        "sql": "ALTER TABLE \"public\".\"users\" ADD COLUMN \"avatarUrl\" text"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"avatarUrl\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "users",
          "avatarUrl"
        ]
      }
    ]
  }
]